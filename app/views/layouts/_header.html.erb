    <header class="navbar navbar-fixed-top navbar-inverse custom_navbar">
      <div class="container">
        <div class="logo">
          <%= link_to "<span class='top'>Start</span><br>
          <span class='larger bottom'>This!</span>".html_safe, root_path %>
        </div>
        <div class="tagline">
          <small>Free ideas to change the world.<br>
            By Wikipedia’s co-founder and friends.
            <span>You in?</span></small>
        </div>
        <nav class="header_nav nav navbar-nav navbar-right">
          <ul>
            <% if logged_in? %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <%= current_user.name %> <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Profile", current_user %></li>
                  <li><%= link_to "Settings",
                                  edit_user_path(current_user) %></li>
                  <li class="divider"></li>
                  <li><%= link_to "Log out",
                                  logout_path, method: "delete" %></li>
                </ul>
              </li>
              <li><%= link_to "Members", users_path %></li>
            <% else %>
              <li><%= link_to "Log in", login_path %></li>
            <% end %>
            <li><%= link_to "About Us", about_path %></li>
            <li><%= link_to "Help and FAQ", help_path %></li>
          </ul>
        </nav>
      </div>
    </header>
